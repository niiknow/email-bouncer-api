version: '3'

services:
  bouncer:
    image: php:8.2-apache
    ports:
      - 8888:80
    volumes:
      - .:/var/www
    command:
      [
        "bash",
        "-c",
        "a2enmod rewrite && chmod 775 /var/www/public && chmod 775 /var/www/storage && apache2-foreground",
      ]
